1. Transport layer:
    source post is generated by computer

    destination port decide which type of service you are need.
        like for email , smtp used which port no . 25
        same for https port no. 443

    Sequence no is used to assemvle ans reassembe the data
        segment no are stored in sequence no. if any seg. miss , it send notification to resent
    
    acknowledgement no : after receiving each packed receiver provide ack. if is not sent ack. then stop sending

    flag : urg(urgent) , RST(reset), ACK(acknowledgement) , SYN  , FIN(terminate connection) fin bit : 1


Data Link Layer 
    DLL two part : LLC and MAC
    LLC maintain connectectivity b/w  data link layer with network layer


ping ( works ON ICMP )
    in this it first sent arp-req to get MAC  if it get through arp-reply then sent ICMP pkt . it require remote server mac adddress.

tracRoute ( also work on ICMP)

Router doesn't forward broadcast

DHCP is used to get IP Address automatically

