1.string to integer
    String str = "123";
    int num = Integer.parseInt(str);
    System.out.println(num); 

    Throws NumberFormatException if the string is not a valid number.
    String invalid = "12a";
    // int num = Integer.parseInt(invalid);  // Throws NumberFormatException

2. To check whether a given string is a substring of another string in Java
    String main = "hello world";
    String sub = "world";

    if (main.contains(sub)) {
        System.out.println("Yes, it is a substring.");
    } else {
        System.out.println("No, it's not a substring.");
    }

3. To make a StringBuilder empty in Java
    sb.setLength(0); // Now sb is empty