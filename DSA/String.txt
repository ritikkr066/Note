Method 1: Convert to Strings and Use compareTo()
    int result = sb1.toString().compareTo(sb2.toString());

Remove a character at a specific index :  use the .deleteCharAt(index) method.
    sb.deleteCharAt(1);

3. Remove all occurrences of a character
    String result = str.replace("l", "");
 
To replace a character in a StringBuilder in Java : .setCharAt(int index, char ch)
    sb.setCharAt(1, 'a');     